{
    "name": "fullstack-example-template",
    "version": "1.0.0",
    "scripts": {
        "start": "npm install --prefix server && npm install --prefix client && npm run build --prefix client && node server/app.js"
    }
}